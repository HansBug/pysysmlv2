Compatibility
=============

The package targets CPython 3.7 through 3.14 on Linux, Windows, and macOS. Generated parser code is included in distributions, so Java is required only for maintainer-side grammar regeneration. The CI matrix uses every Python version on Linux and the versions available on each current Windows and macOS runner.
