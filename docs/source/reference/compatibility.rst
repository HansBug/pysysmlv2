Compatibility
=============

The package targets CPython 3.7 through 3.14 on Linux, Windows, and macOS. Generated parser code is included in distributions, so Java is required only for maintainer-side grammar regeneration. CI covers the complete 3-by-8 operating-system/Python-line matrix. Exact patch versions vary by runner where the official setup-python manifest publishes different legacy artifacts.
