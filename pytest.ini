[pytest]
testpaths = test
python_files = test_*.py
addopts = -ra
markers =
    unit: source-mirrored unit tests
    integration: cross-module integration tests
    conformance: official or derived grammar conformance tests
